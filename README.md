# MathsStudy

A website for studying for SQA exams via (deterministically) generated questions instead of the limited supply of past papers.
Starting with only one subject (maths) at one level (national 5), the website will be split into topics where users can read study notes and answer questions, both rendered trhough latex.

I will use React for the front end, Node for the back end and MySQL for databasing.